# CBZ Automation Suite — Python dependencies
# All other imports are Python standard library (zipfile, re, pathlib, logging, etc.)

# Required by cbz_watcher.py only
watchdog>=3.0.0

# Translation and offline transliteration for Japanese/Chinese/Korean metadata
googletrans>=4.0.2
Unidecode>=1.3.8
truststore>=0.10.4
